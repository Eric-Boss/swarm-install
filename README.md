# swarm installer

Source is private. This repo is the public tarball + `install.sh` so beta
users can run:

```bash
curl -fsSL https://raw.githubusercontent.com/Eric-Boss/swarm-install/main/install.sh | bash
```

Then `swarm doctor`.
